name: pocketapps_auth
description: "Shared authentication package for PocketApps apps (Supabase + Google sign-in)."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.16.0
  flutter_riverpod: ^3.3.2
  go_router: ^17.3.0
  google_sign_in: ^7.2.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
